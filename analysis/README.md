# 推薦ルールの検討

`node analysis/generate-weapon-evidence.mjs` を実行すると、推薦順位を保存せずに、能力値・閃き・加入時技Lv補正が対立する代表例を表として出力します。

この分析はスコア導入前の比較資料です。現在の道中スコアと表示ルールは `site/formula.html`、実装は `site/logic/recommendations.js` を参照してください。
